import { MatriculaFormatoDirective } from './matricula-formato.directive';

describe('MatriculaFormatoDirective', () => {
  it('should create an instance', () => {
    const directive = new MatriculaFormatoDirective();
    expect(directive).toBeTruthy();
  });
});
